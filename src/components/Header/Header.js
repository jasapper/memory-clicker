import React from "react";
import "./Header.css";

const Header = props => (
  <header className="header">
    <h1>Memory Clicker!</h1>
    <h2>
      Click on an image to earn points, but don't click the same image/character more than once!
    </h2>
  </header>
);

export default Header;
